<div class="container">
  <%= provide(:title, t('purchases.name')) %>
  <h1><%= t('purchases.view.title_edit') %>></h1>

  <%= render 'form', purchase: @purchase %>
</div>

<div class="container btn-container">
  <%= render 'shared/link_default', { path: purchases_path, title: t('button.back') } %>
  <%= render 'shared/link_default', { path: @purchase, title: t('button.show') } %>
</div>

