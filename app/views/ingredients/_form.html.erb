<% if ingredient.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

    <ul>
    <% ingredient.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form.hidden_field :dish_id, id: :ingredient_dish_id, value: @dish.id %>

<div class="form-group">
  <%= form.label :product_id %>
  <%= form.collection_select(:product_id, Product.all, :id, :name_with_unit,
                             {
                                 include_blank: true
                             },
                             class: 'form-control'
      )
  %>
</div>

<div class="field">
  <%= form.label :quantity %>
  <%= form.number_field :quantity, id: :ingredient_quantity %>
</div>

<div class="actions">
  <%= form.submit %>
</div>

